{
  "private": true,
  "type": "module",
  "scripts": {
    "test:js": "node --check app.js && node test_visual_model.mjs",
    "test:smoke": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
